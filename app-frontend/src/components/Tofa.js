import React, {Component} from 'react';
import {withRouter} from "react-router";

class Tofa extends Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

export default withRouter(Tofa);
